# Day 27: Git Revert HEAD Commit (Games Project)

## Scenario
The Nautilus application development team is maintaining a repository at `/usr/src/kodekloudrepos/games` on the Storage server in Stratos DC.  

They reported an issue with the most recent commits and requested the DevOps team to **revert the HEAD commit back to the previous commit**.  

Requirements:
1. Revert the latest commit (HEAD) to the previous commit.  
2. The previous commit should be the one with the **initial commit message**.  
3. Use the commit message:  
